# Roadmap (Fabric doc alias)

This is a placeholder document for the Fabric docs alias `roadmap`.

- Configure `fabric.docs_root` to point at a directory containing your Fabric docs.
- Then run: `fabric-open-doc roadmap`

